name: Playwright Cucumber Tests

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      tags:
        description: 'Cucumber tag(s) to run, optional (e.g. @auth)'
        default: ''

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run Playwright Cucumber tests
        run: npm run test:with-report1
        env:
          BROWSER: ${{ matrix.browser }}
          npm_config_tags: ${{ github.event.inputs.tags }}

      # 🔹 Generate Allure report explicitly
      - name: Generate Allure Report
        run: |
          mkdir -p allure-report
          allure generate allure-results --clean -o allure-report

      # 🔹 Upload Allure Report artifact
      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ matrix.browser }}
          path: allure-report

      # 🔹 Publish Allure report to GitHub Pages (only Chromium)
      - name: Deploy Allure report to GitHub Pages
        if: ${{ matrix.browser == 'chromium' }}
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report
